REPOSITORY TAILORED TO MY PERSONAL USE

Required steps:
- Set SPOTIFY_CLIENT_ID and SPOTIFY_CLIENT_SECRET in the env. variables
- Create a fsx that loads the modules to run the program without passing through the console